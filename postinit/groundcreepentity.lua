GLOBAL.setfenv(1, GLOBAL)

local GroundCreepEntity = GroundCreepEntity

local creepentity_parent = {}
local creepentity_real = {}

local _SetRadius = GroundCreepEntity.SetRadius
--function GroundCreepEntity:SetRadius(rad)

--end

